def calcular_soma_fracao(n):
    if n <= 0:
        while n <= 0:
            n = int(input("Por favor, digite um valor positivo para 'n': "))
    
    soma = 0.0
    for i in range(2, n + 2):
        soma += i / (2 * i - 1)
    
    return soma
